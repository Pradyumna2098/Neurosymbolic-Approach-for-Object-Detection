# FastAPI Backend Requirements
# For the neurosymbolic object detection API prototype

# Web Framework
fastapi==0.109.1  # Security: Fixed ReDoS vulnerability (CVE-2024-24762)
uvicorn[standard]==0.27.0

# Data Validation
pydantic==2.5.3
pydantic-settings==2.1.0

# File Upload Support
python-multipart==0.0.22  # Security: Fixed arbitrary file write, DoS, and ReDoS vulnerabilities

# Environment Configuration
python-dotenv==1.0.0

# Image Processing
Pillow==10.3.0  # Security: Fixed buffer overflow vulnerability

# Testing Dependencies
pytest==7.4.3
httpx==0.26.0  # For TestClient async support

# JSON handling (built-in but explicit version)
# Standard library - no need to specify

# Machine Learning & Computer Vision (for inference service)
torch==2.6.0  # Security: Fixed RCE vulnerability (CVE-2024-XXXXX) - requires weights_only=True protection
torchvision==0.21.0  # Compatible with torch 2.6.0
sahi==0.11.36
ultralytics==8.2.77
opencv-python==4.10.0.84
pyyaml==6.0.1
